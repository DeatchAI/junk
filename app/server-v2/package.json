{
  "name": "detach-runtime",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.ts",
    "sync:opencode": "bun run scripts/sync-opencode.ts",
    "build": "bun run sync:opencode && bun build --compile --outfile detach-runtime src/index.ts && codesign --force --options runtime --entitlements entitlements.plist --sign - detach-runtime",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run test && bun run typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21"
  },
  "module": "src/index.ts",
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@composio/core": "0.13.1",
    "opencode-ai": "1.18.10",
    "pdfjs-dist": "5.4.149"
  }
}
