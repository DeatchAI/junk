# User request: ignore the server-deprecated dir
server-deprecated-use-v2/

# Generated runtime binaries are built locally and in release CI. They are not
# source artifacts and must not be committed.
app/lazzy/detach-runtime
app/server-v2/detach-runtime
app/lazzy/opencode

# Xcode specific ignores
xcuserdata/
*.xcuserdatad
buildServer.json
DerivedData/
*.xcresult

# Node dependencies and build folders
node_modules/
.next/
out/
dist/
build/
.vercel/

# Environment files
.env*
!.env.example

# The website is maintained in a separate private repository.
web/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
